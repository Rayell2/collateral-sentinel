[project]
name = "collateral-sentinel"
authors = ["Sentinel Team"]
description = "A decentralized collateral management and tracking system"
telemetry = false
requirements = []
[contracts.sentinel-vault]
path = "contracts/sentinel-vault.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
